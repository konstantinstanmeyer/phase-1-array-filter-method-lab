
function findMatching(input, find){
    return input.filter(possibleMatch => possibleMatch.toLowerCase() === find.toLowerCase())
}

function fuzzyMatch(){
    const result = 0;
}

function matchName(){
    const result = 0;
}